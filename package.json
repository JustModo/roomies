{
  "name": "watch-party-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.10.3",
    "typescript": "^5.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
