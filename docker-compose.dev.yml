services:
  caddy:
    image: caddy:2
    ports:
      - "5123:5123"
    volumes:
      - ./infra/caddy/Caddyfile.dev:/etc/caddy/Caddyfile:ro
      - ${ROOMIES_MEDIA_DIR:-./media}:/media:ro
      - ${ROOMIES_CACHE_DIR:-./cache}:/cache
    extra_hosts:
      - "host.docker.internal:host-gateway"
