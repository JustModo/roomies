services:
  caddy:
    image: caddy:2
    ports:
      - "5123:5123"
    volumes:
      - ./infra/caddy/Caddyfile.dev:/etc/caddy/Caddyfile:ro
      - ./media:/media:ro
      - ./cache:/cache
    extra_hosts:
      - "host.docker.internal:host-gateway"
